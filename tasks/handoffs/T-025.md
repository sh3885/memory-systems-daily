Task: T-025
Status: done
Files changed:
- automation/telegram/lesson-command-router.mjs
- automation/telegram/worker.mjs
- automation/storage/d1-lesson-store.mjs
- automation/tests/lesson-command-router.test.mjs
- docs/NEXT_STEPS.md
- tasks/BOARD.md
Decisions:
- Added `/status` so Telegram can report the current lesson, revision, active approval, latest publication, and recommended next action.
- Added `/commands` as an alias for `/help`.
- Added `/publish-retry` for approved or publish_failed lessons. The Worker resolves the current active approval from D1 and reuses the publication service.
- Rewrote Telegram command-router user-facing Korean strings as valid UTF-8 because the existing text was visibly corrupted.
- Kept publishing retry approval-gated. It does not publish from a raw Telegram command unless D1 still has an active approval matching the current revision.
Validation:
- `npm.cmd run test:automation`: pass, 71 tests.
- `git diff --check`: pass.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: pass, 0 errors, 0 warnings, 0 hints.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1`: pass, Worker version `1882750e-2783-46d4-a2b5-0332f74c4ebf`.
- Signed production webhook smoke test: `/commands` -> `help_sent`, `/status` -> `status_missing`, `/publish-retry` -> `publish_retry_missing_lesson`.
Risks:
- `/publish-retry` still depends on the real GitHub App permissions and an active unexpired approval from a prior approval button.
- A real approval-button publication has not yet been executed end to end in production.
Next action:
- Run the real daily publish flow after the scheduler creates a lesson, or manually trigger/create today's lesson before testing approval-button publishing.
