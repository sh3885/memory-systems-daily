Task: T-021
Status: done
Files changed:
- wrangler.toml.example
- scripts/check-env.ps1
- scripts/set-worker-secrets.ps1
- scripts/set-telegram-webhook.ps1
- scripts/apply-d1-migrations.ps1
- docs/DEPLOYMENT_GUIDE.md
- tasks/BOARD.md
Decisions:
- Keep `.env` local and ignored; scripts report secret presence without printing secret values.
- Use `wrangler.toml.example` instead of committing a real `wrangler.toml` because the Cloudflare D1 database_id is not known locally yet.
- Add `set-worker-secrets.ps1` so Worker secrets can be uploaded from `.env` after Wrangler login and real config creation.
- Add `set-telegram-webhook.ps1` so Telegram can be pointed at the deployed Worker URL with the required secret token.
- Add `apply-d1-migrations.ps1` for ordered D1 schema setup using existing SQL migrations.
Validation:
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-env.ps1`: passed for current manual Telegram mode.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\set-worker-secrets.ps1 -DryRun`: listed expected Worker secrets without uploading.
- `npm.cmd run test:automation`: 63 tests passed.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: Astro check/build passed.
- `git diff --check`: passed.
Risks:
- Real Cloudflare deployment still needs a D1 database_id and Worker URL from the user's Cloudflare account.
- Approval-to-GitHub publishing is not implemented in this task.
Next action:
- Create/fill real `wrangler.toml`, upload Worker secrets, run D1 migrations, deploy Worker, then register Telegram webhook.
