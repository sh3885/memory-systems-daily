Task: T-022
Status: done
Files changed:
- wrangler.toml
- scripts/deploy-worker.ps1
- scripts/apply-d1-migrations.ps1
- .gitignore
- docs/DEPLOYMENT_GUIDE.md
- tasks/BOARD.md
Decisions:
- Created the real Cloudflare Worker config using D1 database_id `8fda3f2c-2ebd-437b-9b19-0d1d395b3d24`.
- Kept Windows instructions on `npx.cmd` because PowerShell execution policy blocks `npx.ps1` for this user.
- Made D1 migration script target remote Cloudflare D1 by default; `-Local` is now explicit.
- Added `deploy-worker.ps1` to deploy with a temporary secrets JSON generated from `.env`, then remove the temporary file.
- Ignored `.wrangler/` because it is local generated Wrangler state.
Validation:
- `npx.cmd wrangler --version`: 4.113.0.
- `npx.cmd wrangler whoami`: logged in and D1/write scopes available.
- `npx.cmd wrangler d1 list`: confirmed `memory-systems-daily-db`.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1 -DryRun`: passed with 18 hidden env bindings.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\apply-d1-migrations.ps1 -DatabaseName memory-systems-daily-db`: applied 4 migrations to remote D1.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1`: deployed Worker version `77304195-5dbd-49d4-a375-78aa79d3779d`.
- `npx.cmd wrangler secret list`: confirmed 18 `secret_text` bindings.
- Remote D1 table query: confirmed expected lesson, revision, approval, claim, conversation, and Telegram update tables.
- Telegram `setWebhook`: registered `https://memory-systems-daily-bot.sh3885-lee.workers.dev/telegram/webhook`.
- Telegram `getWebhookInfo`: ok, pending updates 0, no last error.
- Direct signed webhook smoke test with `/help`: returned `{ ok: true, handled: true, action: "help_sent" }`.
- `npm.cmd run test:automation`: 63 tests passed.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: Astro check/build passed.
- `git diff --check`: passed.
Risks:
- Scheduled 08:30 KST notification has not yet been observed at the real cron time.
- Approval-to-GitHub publishing remains the next unimplemented task.
Next action:
- Send `/today`, `/prompt`, and a normal question to the Telegram bot from the allowed private chat, then implement approval-to-GitHub publishing.
