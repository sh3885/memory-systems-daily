Task: T-031
Status: done

Files changed:
- `automation/telegram/lesson-command-router.mjs`
- `automation/tests/lesson-command-router.test.mjs`
- `tasks/BOARD.md`

Decisions:
- `/review` now sends a clear guidance message when no draft/revision exists instead of surfacing an unhandled router error to the webhook.
- Manual `/draft` on a scheduled lesson now promotes the lesson through `researching` to `draft_ready`, so the next `/review` can create an approval challenge.
- `/review` can repair a scheduled/researching lesson that already has a revision by promoting it to `review_ready`.

Validation:
- `npm.cmd run test:automation`: passed, 75 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed.
- Worker deployed with version `f8eaa78f-4f2f-438f-a4ca-29fb4a827224`.
- Synthetic production webhook checks:
  - `/help`: HTTP 200, `help_sent`.
  - `/today`: HTTP 200, `today_sent`.
  - `/review` without draft: HTTP 200, `review_missing_revision`.

Risks:
- Telegram `getWebhookInfo` keeps the previous 500 message as historical last error until Telegram observes a newer error-free window; pending updates are zero.
- The current 2026-07-23 lesson has no revision yet, so the user must save a draft before an approval button appears.

Next action:
- In Telegram, run `/prompt`, generate a Claude draft, save it with `/draft <Markdown>`, then run `/review` to receive the approval button.
