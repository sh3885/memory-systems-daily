Task: T-001
Status: done
Owner: root-agent

Files changed:
- `AGENTS.md`
- `.codex/config.toml`
- `.codex/agents/*.toml`
- `docs/*.md`
- `tasks/*`
- `content/templates/*`
- `scripts/*.ps1`
- `README.md`
- `.env.example`
- `.worktreeinclude`
- `package.json`

Decisions:
- Use root `AGENTS.md` as the durable collaboration contract.
- Use project-scoped custom agents with read-only researcher/reviewer roles.
- Keep agent depth at one and require disjoint ownership for parallel writes.
- Bind future publication approval to Telegram identity, immutable revision, and content hash.
- Preserve the current `.git` directory until OneDrive restores its metadata.

Sources and assumptions:
- Codex manual sections for subagents, project-scoped custom agents, AGENTS.md, and worktrees.
- Three independent read-only agent reviews of collaboration, content lifecycle, and automation.

Validation:
- `scripts/check.ps1`: 0 errors, 0 warnings, 0 hints; 4 static pages built.
- `scripts/visual-check.ps1`: 4 pages x 2 viewports passed.
- Local site: HTTP 200 at `http://127.0.0.1:4321`.

Risks:
- Git and Codex worktrees remain unavailable until OneDrive hydrates `.git/HEAD` and `.git/config`.
- Telegram, storage, AI, GitHub, and deployment integrations are specifications and backlog items, not implemented services yet.

Next action:
- Restore Git metadata, then implement T-010 and T-011 on separate owned paths.

