Task: T-036
Status: done

Files changed:
- `.env.example`
- `automation/content/draft-quality.mjs`
- `automation/publishing/deployment-verifier.mjs`
- `automation/publishing/publication-service.mjs`
- `automation/telegram/lesson-command-router.mjs`
- `automation/telegram/worker.mjs`
- `automation/tests/draft-quality.test.mjs`
- `automation/tests/lesson-command-router.test.mjs`
- `automation/tests/publication-service.test.mjs`
- `docs/ARCHITECTURE.md`
- `docs/deploy-pages.workflow.yml`
- `docs/OPERATIONS.md`
- `tasks/BOARD.md`
- `tasks/handoffs/T-036.md`

Decisions:
- `published` is recorded only after production URL marker verification succeeds.
- GitHub commit/PR success without Pages verification records a publish failure instead of a false success.
- `/publish-retry`, `/deploy-retry`, and `/verify-url` share the same recovery path.
- `/draft` now rejects likely mojibake, missing H1, missing Claim ledger, and invalid frontmatter category before creating a revision.
- GitHub Actions workflow template builds Astro and deploys `dist` to Cloudflare Pages when installed under `.github/workflows/` and configured with Cloudflare secrets.
- `docs/OPERATIONS.md` explains lesson, publishing, D1, Cloudflare Worker/Pages, commands, and recovery paths.

Validation:
- `npm.cmd run test:automation`: passed, 93 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed.
- Worker deployed: `8f480a1b-ea25-4255-81fa-1e42b3fd5a52`.
- Production post URL verification passed for title and `Claim ledger`.
- Production home page verification passed for post link and title.
- Production LLM category page verification passed for post link and title.
- Production Telegram webhook synthetic `/status` update `990000238`: HTTP 200, `status_sent`.

Risks:
- GitHub Actions deploy requires repository secrets `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN`. The current GitHub App token cannot push `.github/workflows/*` because it lacks workflow-write permission, so the workflow is committed as `docs/deploy-pages.workflow.yml` until a workflow-authorized credential installs it.
- Worker cannot run Astro build itself; it gates `published` on URL verification and relies on GitHub Actions or manual Pages deploy for the build/deploy step.

Next action:
- Install `docs/deploy-pages.workflow.yml` as `.github/workflows/deploy-pages.yml`, add the two Cloudflare secrets to the GitHub repository, then run one end-to-end next-day post to confirm unattended Pages deployment.
