Task: T-011
Status: done
Owner: root-agent

Files changed:
- `automation/domain/lesson-state.mjs`
- `automation/storage/d1-lesson-store.mjs`
- `automation/storage/migrations/0001_lesson_store.sql`
- `automation/tests/lesson-store.test.mjs`
- `automation/tests/node-d1-adapter.mjs`
- `scripts/test-automation.ps1`
- `package.json`
- `docs/ARCHITECTURE.md`
- `docs/PROJECT_STATUS.md`
- `tasks/BOARD.md`

Decisions:
- Use a strict lesson state machine with optimistic state versions.
- Store immutable revisions and canonical SHA-256 hashes over NFC-normalized, LF-delimited UTF-8 content.
- Use `lesson_heads` with a composite foreign key for the current revision pointer.
- Require a pre-issued approval challenge bound to Telegram user, chat, nonce, revision, hash, and expiry.
- Deny generic transitions into `approved` and `publishing`; only approval-gated methods may enter them.
- Make revision and challenge retries idempotent with operation keys and full payload comparison.
- Keep lesson events append-only and Telegram update IDs unique at the database boundary.

Validation:
- `npm run test:automation`: 19 tests passed.
- `scripts/check.ps1`: Astro check reported 0 errors, 0 warnings, 0 hints.
- Astro static build: 4 pages built successfully.
- `git diff --check`: passed.
- Two independent post-implementation agent reviews: no blocking or high findings remain.

Risks:
- Tests run against Node's SQLite adapter; Wrangler local D1 and deployed D1 integration tests remain for T-010.
- Telegram webhook authentication and update claim/mutation transaction boundaries remain for T-010.
- Publication records, GitHub commit, and deployment verification remain for T-013.

Next action:
- Implement T-010 Telegram webhook using this store contract and the existing update/challenge idempotency constraints.

