Task: T-017
Status: done
Files changed:
- `automation/llm/openai-responses-provider.mjs`
- `automation/telegram/worker.mjs`
- `automation/tests/openai-responses-provider.test.mjs`
- `automation/tests/worker-scheduled.test.mjs`
- `docs/PROJECT_STATUS.md`
- `docs/ARCHITECTURE.md`
- `tasks/BOARD.md`

Decisions:
- Added `createStudyResearchProvider` on top of the existing OpenAI Responses client.
- Required research output to be a JSON object with `content`, `changeSummary`, and primary-source `claims`.
- Left claim validation in the existing claim ledger layer so provider output is still checked before storage.
- Updated Worker scheduled flow to run research only when `OPENAI_API_KEY` is configured; otherwise it still sends a lesson notification.
- Tested the full scheduled path with fake OpenAI and Telegram fetch calls.

Validation:
- `npm.cmd run test:automation`: passed, 54 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, Astro check/build clean.
- `git diff --check`: passed.

Sources:
- OpenAI Text generation guide: `https://developers.openai.com/api/docs/guides/text`
- OpenAI Web search guide: `https://developers.openai.com/api/docs/guides/tools-web-search`

Risks:
- JSON-only research output is validated, but the model can still produce weak claims; human review and claim ledger inspection remain required.
- Full date-to-curriculum topic selection is still a placeholder through `DAILY_CURRICULUM_REF`.
- Raw search/source snippets are not persisted separately yet.
- GitHub publishing after approval is not implemented yet.

Next action:
- Implement approval-to-GitHub publishing and add a real curriculum selector for daily rotation.
