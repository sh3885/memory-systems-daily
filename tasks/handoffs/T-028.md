Task: T-028
Status: done
Files changed:
- automation/content/daily-lesson-prompts.mjs
- automation/telegram/lesson-command-router.mjs
- automation/tests/lesson-command-router.test.mjs
- docs/NEXT_STEPS.md
- tasks/BOARD.md
Decisions:
- Added a runtime prompt helper for daily lesson context because the Cloudflare Worker cannot directly consume TypeScript-only Astro curriculum data in Node tests and Worker runtime.
- `/prompt` now includes detailed lesson context when the lesson has a known `curriculumRef`, plus required Markdown calculation table and inline SVG diagram instructions.
- Manual `/revise` now preserves the artifact requirements so revisions do not remove the table or diagram.
- Chose inline SVG as the first image/diagram path because it renders inside Markdown without needing Telegram file download, GitHub asset upload, or Cloudflare R2.
- Documented future external PNG/SVG asset upload as the next step after inline SVG.
Validation:
- `npm.cmd run test:automation`: pass, 72 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: pass, 0 errors, 0 warnings, 0 hints.
- Prompt length smoke: M01-W01-D1 `/prompt` is about 1977 characters, under Telegram's 4096 character limit.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1`: pass, Worker version `16ae409b-f479-4771-aa78-032aa09bc8b8`.
- Signed production webhook smoke test: `/prompt` -> `manual_prompt_sent`.
Risks:
- The runtime helper currently covers the first week in detail. The Astro curriculum already has more day-level data, but Worker runtime needs a shared data-source cleanup to avoid duplication.
- Inline SVG depends on Claude following the prompt. A later quality gate should inspect saved `/draft` content and reject missing table/SVG before `/review`.
Next action:
- Add pre-review validation that checks the saved Markdown contains at least one table and one inline SVG, then expand the runtime daily lesson helper or move curriculum data to a shared `.mjs` source.
