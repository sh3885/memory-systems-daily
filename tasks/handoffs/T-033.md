Task: T-033
Status: done

Files changed:
- `automation/storage/d1-lesson-store.mjs`
- `automation/telegram/lesson-command-router.mjs`
- `automation/telegram/telegram-webhook.mjs`
- `automation/telegram/worker.mjs`
- `automation/telegram/README.md`
- `automation/tests/lesson-command-router.test.mjs`
- `automation/tests/telegram-webhook.test.mjs`
- `automation/tests/lesson-store.test.mjs`
- `tasks/BOARD.md`
- `tasks/handoffs/T-033.md`

Decisions:
- Approval button callback data now uses `approve:<opaque-token>` instead of embedding the long challenge id.
- The webhook resolves short approval tokens through the pending challenge lookup in D1.
- Re-running `/review` invalidates older pending approval challenges before issuing a fresh one.
- Failed update handling now completes the Telegram update even when rejection notification sending fails.

Validation:
- `npm.cmd run test:automation`: passed, 84 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed.
- `npx.cmd wrangler deploy`: deployed Worker version `8ba4ee72-0ca2-44b4-8aa4-555c0171a9a5`.
- Production synthetic `/review` update `990000234`: HTTP 200, `review_ready_with_approval`.
- Production D1: update `990000234` has `handled_at`; challenge `challenge_b84dc540-9b2f-42a5-94a2-43303ca4b637` is pending; previous challenge `challenge_a23dfc20-ab62-49b5-a620-794ef8d65ef7` is invalidated with `new_approval_prompt`.

Risks:
- Older failed Telegram updates from before this fix remain in `processed_telegram_updates` with `handled_at` null, but future redelivery can be reclaimed after the lease and new `/review` messages are handled correctly.

Next action:
- User should use the latest approval button generated after this deployment, or send `/review` again to create a fresh valid button.
