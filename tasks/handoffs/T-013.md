Task: T-013
Status: done
Files changed:
- automation/publishing/astro-post.mjs
- automation/publishing/github-app-publisher.mjs
- automation/publishing/publication-service.mjs
- automation/storage/d1-lesson-store.mjs
- automation/storage/migrations/0005_publications.sql
- automation/telegram/worker.mjs
- automation/tests/publication-service.test.mjs
- automation/tests/publication-store.test.mjs
- scripts/apply-d1-migrations.ps1
- scripts/deploy-worker.ps1
- scripts/set-worker-secrets.ps1
- docs/DEPLOYMENT_GUIDE.md
- tasks/BOARD.md
Decisions:
- Publish approved revisions as Markdown pages under `GITHUB_CONTENT_DIRECTORY`.
- Use GitHub App installation tokens from Worker secrets.
- Publish to `GITHUB_CONTENT_BRANCH` and create or reuse an open PR against the repository default branch.
- Record immutable publication rows in D1, then transition lessons to `published` or `publish_failed`.
- Keep failure operation keys per publishing state version so a failed approval can be retried safely.
- Use `PUBLIC_SITE_URL` only when present; otherwise the publication URL falls back to the PR URL.
Validation:
- `npm.cmd run test:automation`: 69 tests passed.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: Astro check/build passed.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1 -DryRun`: passed with hidden Worker bindings.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\apply-d1-migrations.ps1 -DatabaseName memory-systems-daily-db -MigrationFilter "0005_*.sql"`: applied remote D1 publication migration.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1`: deployed Worker version `af284b33-ef56-4bb2-a179-b1a01b8f6f82`.
- Telegram `getWebhookInfo`: ok, pending updates 0, no last error.
- Direct signed webhook smoke test with `/help`: returned `{ ok: true, handled: true, action: "help_sent" }`.
- `git diff --check`: passed.
Risks:
- A real approval-button publish has not been used on production content yet; the implementation is covered by local tests and deployed Worker smoke tests.
- `PUBLIC_SITE_URL` is still missing, so publication records will use the GitHub PR URL until a public Astro hosting URL is configured.
Next action:
- Run the normal Telegram flow: `/today`, `/prompt`, `/draft ...`, `/review`, approval button. Confirm the created GitHub PR.
