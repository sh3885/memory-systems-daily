Task: T-020
Status: done
Files changed:
- `.env.example`
- `automation/llm/anthropic-messages-provider.mjs`
- `automation/telegram/lesson-command-router.mjs`
- `automation/telegram/worker.mjs`
- `automation/tests/anthropic-messages-provider.test.mjs`
- `automation/tests/lesson-command-router.test.mjs`
- `automation/tests/worker-scheduled.test.mjs`
- `docs/PROJECT_STATUS.md`
- `docs/ARCHITECTURE.md`
- `tasks/BOARD.md`

Decisions:
- Changed the active Worker configuration to Claude-only.
- Made `AI_MODE=manual` the default.
- Added `/prompt`, manual question prompts, `/draft`, and manual `/revise` prompts for Claude web usage without API cost.
- Added explicit opt-in API commands: `/ask-api` and `/revise-api`.
- Removed OpenAI fallback from the active Worker wiring. Existing OpenAI provider modules remain in the repo but are not part of the default active flow.
- Scheduled daily flow now creates the lesson and sends manual Claude prompt guidance instead of auto-generating an API draft.

Validation:
- `npm.cmd run test:automation`: passed, 63 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, Astro check/build clean.
- `git diff --check`: passed.

Risks:
- Manual mode requires copy/paste between Telegram and Claude web.
- `/ask-api` and `/revise-api` still require Anthropic API billing and `ANTHROPIC_API_KEY`.
- Claim ledger is not automatically populated in pure manual mode yet.

Next action:
- Add manual claim entry/import commands or continue to approval-to-GitHub publishing.
