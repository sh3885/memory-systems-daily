Task: T-038
Status: done
Files changed:
- `.github/workflows/deploy-pages.yml`
- `tasks/BOARD.md`
- `tasks/handoffs/T-038.md`
Decisions:
- Changed the Cloudflare Pages deploy step from `npx wrangler ...` to `npx --yes wrangler@latest ...` so GitHub Actions can install and run Wrangler without an interactive prompt.
Validation:
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, 0 Astro diagnostics and successful static build.
- The previous GitHub Actions run reached the deploy step, proving checkout, setup, install, and build completed successfully.
Risks:
- If the next deploy step fails, the remaining likely causes are incorrect GitHub secret values or insufficient Cloudflare API token permissions.
Next action:
- Push the fix and check the next GitHub Actions deploy run.
