Task: T-026
Status: done
Files changed:
- src/data/curriculum.ts
- docs/NEXT_STEPS.md
- tasks/BOARD.md
Local ignored changes:
- `.env` `DAILY_CURRICULUM_REF` was changed from the previous module-5 reference to `M01-W01-D1`.
Decisions:
- Rewrote the 36-week curriculum as readable UTF-8.
- Put LLM fundamentals and memory traffic in module 1 so the first posts start from "next token as data movement."
- Kept the path broad for a DRAM-company engineer: CPU/DRAM path, DRAM controller, DDR/LPDDR/GDDR/HBM, GPU/accelerator memory hierarchy, LLM training, LLM serving, distributed AI, I/O, CXL, emerging memory, and latest LLM trends.
- Rewrote the weekly rhythm around repeatable daily posting categories.
Validation:
- `npm.cmd run test:automation`: pass, 71 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: pass, 0 errors, 0 warnings, 0 hints.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\visual-check.ps1`: pass, 4 pages x 2 viewports.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-pages.ps1`: pass, deployed preview `https://f0bcf9e6.memory-systems-daily.pages.dev`.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\deploy-worker.ps1`: pass, Worker version `0d27df36-6ae0-4144-9f47-ec6e6968b076`.
- Production smoke: `https://memory-systems-daily.pages.dev/` and `/roadmap/` returned HTTP 200 and contained the new curriculum text.
Risks:
- Per-day lesson expansion is still not implemented. The Worker still uses one configured `DAILY_CURRICULUM_REF` instead of selecting the next day from the curriculum automatically.
- Some visible Korean text outside `src/data/curriculum.ts` still has old encoding artifacts.
Next action:
- Add a curriculum selector that maps dates to Mxx-Wxx-Dx automatically, then clean remaining UI text encoding artifacts.
