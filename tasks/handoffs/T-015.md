Task: T-015
Status: done
Files changed:
- `automation/telegram/telegram-client.mjs`
- `automation/telegram/lesson-command-router.mjs`
- `automation/telegram/telegram-webhook.mjs`
- `automation/telegram/worker.mjs`
- `automation/telegram/README.md`
- `automation/tests/telegram-client.test.mjs`
- `automation/tests/lesson-command-router.test.mjs`
- `.env.example`
- `docs/PROJECT_STATUS.md`
- `docs/ARCHITECTURE.md`
- `tasks/BOARD.md`

Decisions:
- Added a small Telegram Bot API client for `sendMessage` and `answerCallbackQuery`.
- Added a command router for `/today`, `/revise`, `/review`, `/help`, and plain-text Q&A.
- Kept Q&A and revision generation provider-injected so the next task can attach an LLM/search provider without changing transport tests.
- Wired Worker `fetch` to the router and Worker `scheduled` to send the daily Telegram lesson notification.
- Used approval challenge nonce hashing for approval button callback verification; the Worker explicitly opts into resolving callback token as the nonce.

Validation:
- `npm.cmd run test:automation`: passed, 45 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, Astro check/build clean.
- `git diff --check`: passed.

Risks:
- The real LLM/search provider is still a fallback stub.
- Scheduled curriculum selection temporarily uses `DAILY_CURRICULUM_REF`; a full date-to-curriculum selector is still needed.
- No durable conversation ledger exists yet for every Q&A turn.
- GitHub publishing after approval is not implemented yet.

Next action:
- Implement the LLM/search provider and conversation ledger, then connect approved revisions to GitHub publishing.
