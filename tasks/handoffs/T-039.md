Task: T-039
Status: done
Files changed:
- `.github/workflows/deploy-pages.yml`
- `tasks/BOARD.md`
- `tasks/handoffs/T-039.md`
Decisions:
- Replaced the workflow's `CLOUDFLARE_ACCOUNT_ID` secret reference with the Cloudflare account ID verified by local Wrangler.
- Kept `CLOUDFLARE_API_TOKEN` as a GitHub Actions secret.
- This removes one manual secret value that was easy to confuse with the D1 database ID.
Validation:
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, 0 Astro diagnostics and successful static build.
- Prior GitHub Actions run failed only in the Cloudflare deploy step with Cloudflare API code 7003, indicating the account identifier was invalid.
Risks:
- If the next run fails, the likely remaining issue is the Cloudflare API token value or its `Account / Cloudflare Pages / Edit` permission.
Next action:
- Push the fix, inspect the next GitHub Actions run, then verify the production URL.
