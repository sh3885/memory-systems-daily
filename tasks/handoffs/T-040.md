Task: T-040
Status: done
Files changed:
- `.github/workflows/deploy-pages.yml`
- `.env.example`
- `wrangler.toml`
- `automation/blog/*`
- `automation/publishing/admin-post.mjs`
- `automation/publishing/github-content-writer.mjs`
- `automation/storage/migrations/0006_blog_operations.sql`
- `automation/telegram/worker.mjs`
- `automation/tests/blog-api.test.mjs`
- `public/scripts/*`
- `scripts/deploy-worker.ps1`
- `scripts/visual-check.mjs`
- `src/components/PostList.astro`
- `src/data/blog.ts`
- `src/data/blog-settings.json`
- `src/layouts/*`
- `src/pages/*`
- `src/styles/global.css`
Decisions:
- Reworked the public site into a list-first technical blog with category streams, sidebar widgets, clickable tags, tag index/detail pages, related posts, and visitor stat slots.
- Added D1-backed visit counters through Worker `/api/visits`.
- Added `/admin` as a browser publishing surface. The browser stores only the Worker API URL and `ADMIN_API_TOKEN`; GitHub credentials stay server-side in Worker secrets.
- Admin publishing writes Markdown files directly to `main` through the GitHub App content writer, so GitHub Actions deploys the new post automatically.
- Telegram approval publishing remains on the existing `content/daily` approval-gated path.
- Set `PUBLIC_BLOG_API_BASE` in the Pages workflow to the deployed Worker URL.
Validation:
- `npm.cmd run test:automation`: passed, 96 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, 0 Astro diagnostics and 20 static pages built.
- `VISUAL_BASE_URL=http://127.0.0.1:4321 powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\visual-check.ps1`: passed, 7 pages x 2 viewports.
- Remote D1 blog tables applied by direct `wrangler d1 execute` of `0006_blog_operations.sql`.
- Worker deployed: `https://memory-systems-daily-bot.sh3885-lee.workers.dev`, version `3a705ea9-51af-4461-8142-44b5221bedac`.
- Remote `/api/health`, `/api/visits`, and authenticated `/api/admin/posts` smoke checks passed.
- CORS preflight passed for `http://127.0.0.1:4321` and `https://memory-systems-daily.pages.dev`.
Risks:
- Wrangler migration history does not know about 0001-0005 on the existing D1 database, so 0006 was applied by direct SQL execution instead of `wrangler d1 migrations apply`.
- Web admin publish was validated with tests and authenticated list smoke checks, but no real sample post was created to avoid adding unwanted content.
Next action:
- Push to `origin/main`, let GitHub Actions deploy Pages, then verify production home, tags, admin, and visitor counter behavior.
