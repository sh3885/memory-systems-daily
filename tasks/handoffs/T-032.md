Task: T-032
Status: done

Files changed:
- `automation/telegram/telegram-client.mjs`
- `automation/telegram/lesson-command-router.mjs`
- `automation/tests/telegram-client.test.mjs`
- `automation/tests/lesson-command-router.test.mjs`
- `tasks/BOARD.md`

Decisions:
- Added Telegram `getFile` and file download support to the bot client.
- Added `.md` / `.markdown` document handling in the lesson router.
- A Markdown document with no caption, or with caption `/draft`, is saved as the current lesson draft.
- Unsupported document types and oversized files return guidance instead of failing silently.
- Draft upload size is capped at 512 KB.

Validation:
- `npm.cmd run test:automation`: passed, 80 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed.
- Worker deployed with version `8fce9c1e-eede-4591-93ca-59d90d59af41`.
- Production synthetic `/help` webhook check returned HTTP 200.

Risks:
- End-to-end Telegram file upload was not synthetically exercised because Telegram file downloads require a real `file_id`; unit tests cover the API sequence and router behavior.

Next action:
- In Telegram, upload the Claude `.md` file to the bot with caption `/draft`, then run `/review`.
