Task: T-014
Status: done
Owner: root-agent

Files changed:
- `automation/scheduler/daily-lesson-scheduler.mjs`
- `automation/tests/daily-lesson-scheduler.test.mjs`
- `docs/PROJECT_STATUS.md`
- `tasks/BOARD.md`

Decisions:
- Treat `30 23 * * *` as the UTC schedule corresponding to 08:30 Asia/Seoul.
- Resolve the lesson date with `Intl.DateTimeFormat` and an explicit `Asia/Seoul` timezone.
- Inject curriculum selection so the scheduler does not duplicate or hard-code the full TypeScript curriculum.
- Rely on the D1 lesson store's unique lesson date and schedule conflict behavior for retry idempotency.
- Reject unexpected cron expressions and missing curriculum selection.

Validation:
- `npm.cmd run test:automation`: 30 tests passed.
- Verified UTC-to-KST date boundaries, including year rollover.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: pending after this documentation-only update.

Risks:
- Cloudflare cron trigger and D1 binding are not deployed yet.
- Curriculum selection policy is injected but not yet connected to a daily lesson plan.

Next action:
- Implement T-012 research/Q&A pipeline or connect the scheduler to the Worker runtime after the curriculum selection policy is decided.

