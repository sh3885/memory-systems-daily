Task: T-010
Status: done
Owner: root-agent

Files changed:
- `automation/telegram/telegram-webhook.mjs`
- `automation/telegram/worker.mjs`
- `automation/telegram/README.md`
- `automation/storage/d1-lesson-store.mjs`
- `automation/tests/telegram-webhook.test.mjs`
- `.env.example`
- `docs/ARCHITECTURE.md`
- `docs/PROJECT_STATUS.md`
- `tasks/BOARD.md`

Decisions:
- Keep the Telegram transport boundary independent from Telegram API calls and AI logic.
- Verify method, path, webhook secret, exact user ID, exact chat ID, and private-chat type before claiming an update.
- Claim `(bot_id, update_id)` atomically with a short lease so concurrent delivery runs the handler once.
- Bind approval callbacks to a server-side resolver; the default resolver fails closed and never trusts client callback data as a nonce.
- Record deterministic callback rejection as handled so Telegram does not retry a known-invalid approval forever.

Validation:
- `npm.cmd run test:automation`: 26 tests passed.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: 0 errors, 0 warnings, 0 hints; 4 Astro pages built.
- `node.exe --check` passed for webhook and Worker entrypoint.
- `git diff --check` passed.

Risks:
- `worker.mjs` acknowledges messages but does not yet call Telegram `sendMessage` or an LLM tutor.
- Callback token resolver and Telegram API client remain future implementation work.
- Cloudflare Wrangler/D1 deployment tests remain pending.

Next action:
- Implement T-014 08:30 KST scheduler and T-012 research/Q&A pipeline on separate owned paths.

