Task: T-041
Status: done
Files changed:
- `src/pages/admin.astro`
- `public/scripts/admin.js`
- `src/styles/global.css`
- `scripts/visual-check.mjs`
- `tasks/BOARD.md`
Decisions:
- Added a lightweight `/admin/` entry password gate before showing posting controls.
- Compared the password via a SHA-256 hash in browser JavaScript instead of committing the plaintext password.
- Kept the existing `ADMIN_API_TOKEN` flow unchanged for actual Worker API authorization.
- Updated the admin script to match the current `data-*` based admin page structure and retained post publishing, preview, admin post list, and blog settings updates.
Validation:
- `npm.cmd run test:automation`: passed, 96 tests.
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, 0 Astro diagnostics and successful static build.
- `VISUAL_BASE_URL=http://127.0.0.1:4323 powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\visual-check.ps1`: passed, 7 pages x 2 viewports.
- Playwright gate check: wrong password kept admin content hidden; configured password revealed the post form.
Risks:
- This is a convenience gate, not a real security boundary. The Worker API token remains the real publishing guard.
Next action:
- Commit, push, and verify the deployed `/admin/` page.
