Task: T-037
Status: done
Files changed:
- `.github/workflows/deploy-pages.yml`
- `tasks/BOARD.md`
- `tasks/handoffs/T-037.md`
Decisions:
- Installed the documented Cloudflare Pages direct-upload workflow in the live GitHub Actions path.
- The workflow builds the Astro blog with Node 24, then deploys `dist` to the `memory-systems-daily` Cloudflare Pages project with `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` repository secrets.
- The workflow runs on pushes to `main` and `content/daily`, and can also be triggered manually with `workflow_dispatch`.
Validation:
- `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\check.ps1`: passed, 0 Astro diagnostics and successful static build.
Risks:
- GitHub push may require the GitHub App or token to have `Workflows: Read and write` permission because this task adds `.github/workflows/deploy-pages.yml`.
- The first cloud deployment depends on the newly added GitHub repository secrets being named exactly `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN`.
Next action:
- Push to `origin/main`, then confirm the GitHub Actions deploy run and production URL.
