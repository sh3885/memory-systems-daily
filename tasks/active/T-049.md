---
id: T-049
title: "Recover approval-button issuance after review conflict"
status: done
owner: root agent
role: "Automation Engineer"
curriculumRef: "M01-W01-D1"
dependencies: []
ownedPaths: ["automation/telegram/lesson-command-router.mjs", "automation/tests/lesson-command-router.test.mjs"]
createdAt: 2026-07-23
updatedAt: 2026-07-23
---

# Objective

Make `/review` resilient to a transient optimistic state-version conflict. If
the transition committed as `review_ready`, reload the lesson and issue the
approval challenge instead of returning a generic error without a button.

# Handoff

- Files changed: `automation/telegram/lesson-command-router.mjs`, `automation/tests/lesson-command-router.test.mjs`, and task records.
- Decisions: `/review` now reloads the lesson after a transient `VERSION_CONFLICT`; if the transition already committed as `review_ready`, it proceeds to issue the approval challenge instead of failing without a button.
- Validation result: 100 automation tests passed; `scripts/check.ps1` passed with an Astro production build.
- Risks: repeated conflicts that do not leave the lesson in `review_ready` still surface as an error rather than silently masking a concurrent change.
- Next action: after deployment, send `/review` once for `M01-W01-D1` revision 4 to receive a new approval button.
